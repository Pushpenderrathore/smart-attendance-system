# Smart Attendance System v2 — Python Dependencies
# Install: pip install -r requirements.txt

flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
gunicorn==22.0.0

# All security features (JWT, hashing, rate limiting) use Python stdlib:
# hashlib, hmac, base64, secrets, collections, time — no extra packages needed.
